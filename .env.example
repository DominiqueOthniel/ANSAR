# Local : API Nest
VITE_API_URL=http://localhost:3000/api

# Production Netlify (même site, Functions /api) :
# VITE_API_URL=same
#
# Variables à définir dans Netlify UI (pas dans ce fichier) :
#   DATABASE_URL       = URL Supabase pooler (6543) — même que Koyeb
#   API_FALLBACK_URL   = https://ton-app.koyeb.app  (temporaire)
#   FRONTEND_URL       = https://ton-site.netlify.app
