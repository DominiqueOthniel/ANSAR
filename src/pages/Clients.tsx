import ThirdParties from './ThirdParties';

/** Vue centrée sur les fiches clients (tiers de type client) et leurs détails d’activité. */
export default function Clients() {
  return <ThirdParties scope="clients" />;
}
