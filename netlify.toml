[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA Vite : toutes les routes → index.html
# L’API Nest tourne ailleurs (Render free recommandé) — même Supabase.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
